sudo: enabled
language: python
services:
  - redis
script:
  - pytest -v -s --cov=app --cov-report term-missing
before_script:
  - echo "Menjalankan Testing"
install:
  - pip install -r requirements.txt
before_install:
  - git status